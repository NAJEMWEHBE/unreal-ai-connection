interface:
  display_name: "Unreal Ai Connection"
  short_description: "Repo-specific patterns and workflows for unreal-ai-connection"
  default_prompt: "Use the unreal-ai-connection repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true